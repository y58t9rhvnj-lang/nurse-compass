-- =====================================================================
-- Sprint 3A — ロールバック方針（人間向けメモ）
-- =====================================================================
-- 1. 適用前にセクション A〜D の結果を保存する（特に評価候補 submission_id）。
-- 2. 0019/0020 適用後に問題があれば、アプリを Sprint 2 コミットへ戻し、
--    DB は適用前ダンプから復元するのが最も（DROP 逆順は制約）。
-- 3. 最低限の緊急対応:
--    - アプリを旧 RPC シグネチャ対応へ戻すことは不可（関数は置換済み）。
--    - よって DB とアプリはセットで戻す。
-- 4. assessment_cycles.deadline_at は削除しない（互換残置）。
